#include "main.h"
/**
 * _abs - returns the absolute value of a number
 * @b : integer argument of function
 * Return: returns absolute value of number in integer
 */
int _abs(int b)
{
	if (b >= 0)
		return (b);
	else
		return (-b);
}
