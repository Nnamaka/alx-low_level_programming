#include <stdio.h>
/**
 * main - Code entry
 * Return: Returns 0 after successfull completion
 */
int main(void)
{
	int a, b;

	a, b = 97, 65;

	for (int a = 97; a < 123; a++)
	{
		putchar(a);
	}

	for (int a = 65; a < 91; a++)
		putchar(a);

	return (0);
}
