#include <stdio.h>
/**
 * main - Code entry
 * Return: Returns 0 after successfull completion
 */
int main(void)
{
	int a, b;

	for (a = 97; a < 123; a++)
		putchar(a);

	for (b = 65; b < 91; b++)
		putchar(b);

	return (0);
}
